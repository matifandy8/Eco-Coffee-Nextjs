import {LoadingStyle,  LoaderContainer } from "./loader.styles";

const Loader: React.FC = () => {
  return (
    <LoaderContainer>
    <LoadingStyle></LoadingStyle>
    </LoaderContainer>
  );
};

export default Loader;